import React from 'react';
import '../Styles/Dashboard.css';

const Cashflows = () => {
    return (
        <div className="widget cashflows">
            <h2>Cashflows</h2>
            {/* Cashflow chart with bar representation */}
            <div className="chart"> {/* Chart Content */} </div>
        </div>
    );
};

export default Cashflows;

